card = document.querySelector('div[data-resource-id="<%=@comment.resource_id%>"][data-resource-type="<%=@comment.resource_type%>"]')
commentInput = card.querySelector('#comment_content')
wellElement = card.querySelector('.well')
commentsCounter = card.querySelector('.comments-counter');
commentsCounter.innerText = parseInt(commentsCounter.innerText, 10) + 1;
wellHtml = "<%= j render 'shared/comments_flow', resource: @comment.resource, participation: @participation %>"
commentInput.value = "";
wellElement.innerHTML = wellHtml;
