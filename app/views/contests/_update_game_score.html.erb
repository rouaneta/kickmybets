<%= simple_form_for game do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
  <%= f.input :score_p_one %>
  <%= f.input :score_p_two %>
  <%= f.button :submit, value: 'Update score', data: {confirm: "Really?" } %>
<% end %>
